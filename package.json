{
  "name": "duel-room-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A two-player room-code web game with ephemeral server-side state.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
